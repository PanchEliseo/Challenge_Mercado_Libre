package com.mercadolibre.challenge.presentation.navigation

/**
 * Represent the graph for navigation
 */
object Graph {
    /**
     * Represent the root route composable
     */
    const val ROOT = "root_graph"

    /**
     * Represent the search route composable
     */
    const val SEARCH = "search_graph"

    /**
     * Represent the result route composable
     */
    const val RESULT = "result_graph"
}